我想要构建知识图谱，请从给定的文本中提取核心实体，遵循以下指南：
- 只选择最核心、出现频率最高的2到4个实体
- 将简写的实体名补充为完整形式
- 请提取具体的、有实际意义的实体，避免空泛的概念，实体应该是独立的名词形式

输出格式：
   以 JSON 格式输出结果，结构如下：
   ```json
   {
     "entities": ["实体1", "实体2"...],
   }
   ```

请分析给定的文本，并直接输出json，无需额外的解释说明。