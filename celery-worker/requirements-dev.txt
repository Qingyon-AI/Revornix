# 只有跑测试才需要。这批测试刻意只依赖标准库，因此**不需要**装 requirements.txt ——
# worker 的运行时依赖（torch 等）重到不适合每个 PR 装一遍。
pytest==9.1.1
# 阶段失败语义是 async 的；没有这个插件，async 用例会被当成普通函数收集，
# 拿到一个没跑的 coroutine —— 表现为失败而不是跳过。
pytest-asyncio==1.4.0
